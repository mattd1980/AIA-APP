# Dev: Vite uses this when running npm run dev
VITE_API_URL=http://localhost:3000

# Production (frontend server.js proxy): full backend URL - REQUIRED when frontend is a separate service
# Example: https://your-backend.up.railway.app (no trailing slash)
# If unset or invalid, /api and /health will 404 and the database LED will be red.
BACKEND_URL=
