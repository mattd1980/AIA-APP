[variables]
NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = [
  "cd backend && npm run build",
  "cd ../frontend && npm ci && npm run build",
  "echo '=== Frontend build verification ==='",
  "test -d ../frontend/dist && echo 'Frontend dist directory exists' || echo 'Frontend dist directory missing'",
  "test -f ../frontend/dist/index.html && echo 'Frontend index.html exists' || echo 'Frontend index.html missing'",
  "echo '=== Copying frontend dist to backend ==='",
  "mkdir -p backend/public",
  "cp -r ../frontend/dist/* backend/public/ || echo 'Failed to copy frontend dist'",
  "test -f backend/public/index.html && echo 'Frontend copied successfully' || echo 'Frontend copy failed'"
]

[start]
cmd = "cd backend && npm run railway"
