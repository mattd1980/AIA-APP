# Database
# Railway will automatically provide DATABASE_URL when you add PostgreSQL
DATABASE_URL=postgresql://user:password@host:port/database

# OpenAI API
OPENAI_API_KEY=sk-your-openai-api-key-here
# Vision model for image analysis (default: gpt-5.2; crème de la crème: gpt-5.2-pro; cheaper: gpt-5-mini)
# OPENAI_VISION_MODEL=gpt-5.2

# Server Configuration
PORT=3000
NODE_ENV=production

# Admin: super-admin login for /admin (email: admin@local, password: value below)
ADMIN_PASSWORD=change-me-in-production

# Session (use a long random string in production)
SESSION_SECRET=your-session-secret-change-in-production

# Frontend URL (for CORS and OAuth redirect after login)
# Update this with your frontend deployment URL
FRONTEND_URL=http://localhost:5173

# Backend URL (required for Google OAuth callback when frontend and backend differ)
# Set to your app root in production, e.g. https://ia.heliacode.com
# Google will redirect users to BACKEND_URL/api/auth/google/callback
BACKEND_URL=http://localhost:3000

# Google OAuth (sign in / sign up with Google)
# Create credentials at: https://console.cloud.google.com/apis/credentials
# - Application type: Web application
# - Authorized redirect URI: BACKEND_URL/api/auth/google/callback (e.g. https://your-backend.com/api/auth/google/callback)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: DataForSEO (for pricing data)
# DATAFORSEO_API_KEY=your-key-here
# DATAFORSEO_LOGIN=your-login-here

# Optional: Google Gemini Vision API (adds Gemini models to vision model selector)
# GEMINI_API_KEY=your-gemini-api-key-here
